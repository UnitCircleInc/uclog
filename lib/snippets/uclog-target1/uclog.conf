CONFIG_SERIAL=n
CONFIG_UC_LOG=y
#CONFIG_UC_LOG_SERVER=n

# Disable default config from nrf52840
CONFIG_USE_SEGGER_RTT=n
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_LOG_BACKEND_UART_ASYNC=n
CONFIG_LOG_BACKEND_RTT=n
CONFIG_LOG_BACKEND_UART=n
CONFIG_LOG_MODE_MINIMAL=y   # prevetns subsys/logging/log_core.c from bing used
CONFIG_LOG=y                # needed so can used LOG_CUSTOM_HEADER
CONFIG_LOG_CUSTOM_HEADER=y  # needed so can include using <zephyr/logging/log.h>
